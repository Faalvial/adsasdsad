README primer momento
Al hacer un git push tener en cuenta el .gitignore

Consejo: Crear entorno virtual

Dependencias: opencv-python / insightface / numpy / nnxruntime / psycopg2

Crear base de datos y conectarlo a python

iniciar backend:
uvicorn api.server:app --reload

iniciar frontend:
cd frontend
npm install
npm run dev