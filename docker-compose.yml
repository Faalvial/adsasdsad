services:
  db:
    image: postgres:15
    restart: unless-stopped
    environment:
      POSTGRES_DB: control_asistencia
      POSTGRES_USER: asistencia_user
      POSTGRES_PASSWORD: admin123
      TZ: America/Lima
    # LA LÍNEA MÁGICA QUE SUGERISTE:
    command: postgres -c timezone=America/Lima
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./backend/db/schema.sql:/docker-entrypoint-initdb.d/init.sql

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - DB_HOST=db
      - DB_PORT=5432
      - DB_NAME=control_asistencia
      - DB_USER=asistencia_user
      - DB_PASSWORD=admin123
      - TZ=America/Lima  # En el backend el TZ sí es suficiente para Python
    depends_on:
      - db

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=${VITE_API_URL:-http://localhost:8000}
    depends_on:
      - backend

volumes:
  pgdata: